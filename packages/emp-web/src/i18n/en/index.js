import { en } from 'bwork-libs';
import generic from './generic';
import resources from './resources';
import error from './error';

export default {
  'Internal Server Error': 'Internal Server Error!',
  ra: en,
  generic,
  error,
  resources,
};
